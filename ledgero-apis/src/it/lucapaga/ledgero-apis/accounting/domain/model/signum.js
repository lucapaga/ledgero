'use strict';

// const Schema = require('./schema');

// Declare internals
const internals = {};

/*
{
  value: 0,
  signum: Signum.MINUS,
  currency: Currency.EURO
})
*/

exports = module.exports = internals.Signum = {
  PLUS: "+",
  MINUS: "-"
};
